# sim-racing-wheel
This project relies on the vJoy virtual joystick driver. Specifically, it uses the v2.2.2.0 (Fully Signed Fork), which can be downloaded from vjoy.pro/download/#win11-signed-track. This version is recommended for Windows 11 compatibility.
